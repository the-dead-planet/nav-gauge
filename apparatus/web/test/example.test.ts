import { describe, it } from "mocha";
import { expect } from "chai";

describe("Apparatus web", () => {
    describe("Example test", () => {
        it("should be equal 1", () => {
            expect(1).to.be.equal(1);
        });
    });
});