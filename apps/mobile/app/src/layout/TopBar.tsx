import { FC } from "react";
import { Text, View } from "react-native";

interface Props { }

export const TopBar: FC<Props> = ({ }) => {
    return (
        <View>
            <Text>
                Top bar
            </Text>
        </View>
    );
};
